# Wind Farm Layout Optimization — Temat 2
# =========================================
# pip install -r requirements.txt

# Core
floris>=4.4          # NREL FLORIS — wake modeling & optimization
numpy>=2.0
pandas>=2.0
scipy>=1.12
matplotlib>=3.8

# ERA5 data (Copernicus Climate Data Store)
# Wymaga ~/.cdsapirc z kluczem API: https://cds.climate.copernicus.eu/api-how-to
cdsapi>=0.7
xarray>=2024.1
netcdf4>=1.6

# Streamlit (Etap 2 — frontend)
# streamlit>=1.30    # pip install streamlit
# plotly>=5.18       # pip install plotly

# Development
jupyter>=1.0
ipykernel>=6.0
